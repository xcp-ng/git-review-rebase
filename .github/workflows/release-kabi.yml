name: Create a release from tag - kabi

permissions: {}

on:
  push:
    tags:
      - 'kabi-*'

jobs:
  build:
    name: Build and store python artifacts
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: ./.github/actions/python-build/
        with:
          working-directory: scripts/kabi
          artifact-name: kabi-artifacts

  release:
    name: Create and publish the release
    runs-on: ubuntu-latest
    needs: [build]
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7.0.0
        with:
          name: kabi-artifacts
          path: dist/
      - name: Create release ${{ github.ref_name }}
        run: gh release create ${GITHUB_REF_NAME} --repo ${{ github.repository }} --generate-notes dist/*
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
